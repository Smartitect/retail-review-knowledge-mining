[project]
name = "retail-review-knowledge-mining"
version = "0.1.0"
description = "A demo of various techniques for mining insights from semi structured data."
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Barry Smart", email = "barry.j.a.smart@gmail.com" }
]
license = "MIT"
dependencies = [
    "faker>=40.39.0",
    "ipykernel",
    "numpy>=2.5.3",
    "openai>=3.16.0",
    "pandera[polars]>=0.33.1",
    "plotly>=7.1.0",
    "polars>=1.44.2",
    "python-dotenv>=1.2.3",
    "streamlit>=1.64.0",
    "typesafe-sdk>=0.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "src/jev_classifier",
    "src/retail_model",
    "src/review_charts",
    "src/review_insights",
    "src/review_writer",
    "src/review_wrangler",
]

[dependency-groups]
dev = [
    "nbconvert>=7.17.1",
    "pytest",
    "ruff",
]

[tool.uv]
required-version = ">=0.9.0"
default-groups = ["dev"]
# Let uv download the interpreter rather than requiring one in the image.
python-downloads = "automatic"

[tool.ruff.lint.per-file-ignores]
# The retail data model stores naive UTC timestamps by design (docs/data-model.md),
# so tests build naive datetimes to match it.
"tests/**" = ["DTZ001"]
